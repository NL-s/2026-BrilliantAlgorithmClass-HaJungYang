A clean educational illustration of the 0/1 Knapsack dynamic programming problem. On the left, show 4 labeled items (backpack items like a book, water bottle, snack, and flashlight) each with a weight tag and value tag attached. In the center, show an open backpack with a "MAX 7kg" label. On the right, show a 2D DP table grid being filled row by row, where rows represent items considered and columns represent weight capacity from 0 to 7. Highlight a specific cell being computed with arrows pointing from the "don't take" cell (above) and the "take" cell (diagonal-left shifted by item weight), with a "max( , )" label between them. Color scheme: warm orange for selected items in the backpack, blue for the DP table grid, green for the cell currently being computed and its source arrows, light gray for already-filled cells. The style should feel like a friendly classroom diagram. No dense text, only short labels like "take?" and "don't take" near the arrows.
