A clean educational illustration of the Longest Common Subsequence (LCS) dynamic programming algorithm. Show two strings written horizontally and vertically forming the axes of a 2D DP grid table, for example s1 = "ABCDE" on the vertical axis and s2 = "ACE" on the horizontal axis. Show the grid being filled with numbers, and highlight the diagonal arrows (in green) when characters match (indicating dp[i][j] = dp[i-1][j-1] + 1), and horizontal/vertical arrows (in blue) when characters do not match (indicating taking the max of neighboring cells). After the table is filled, show the backtrace path highlighted in bold gold arrows leading from dp[m][n] back to dp[0][0], tracing out the LCS characters. The matched characters along the diagonal should be circled in green and labeled with the resulting LCS string at the bottom. Style: clean grid diagram on a light background, educational infographic aesthetic. Color scheme: green for matches and backtrace diagonals, blue for non-match arrows, gold for the final LCS path, white grid cells with soft gray borders.
