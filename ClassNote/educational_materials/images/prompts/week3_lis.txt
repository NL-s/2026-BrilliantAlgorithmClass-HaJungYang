A clean educational illustration of the Longest Increasing Subsequence (LIS) dynamic programming algorithm. Show a horizontal row of numbered boxes representing the input array [3, 1, 4, 1, 5, 9, 2, 6]. Below each box, show the corresponding dp value. Highlight the elements that form the LIS (for example [1, 4, 5, 9]) by connecting them with bold upward-curving arrows in green, while non-selected elements are shown in light gray. On the side, show a small dp table being filled with arrows from j to i when arr[j] < arr[i], illustrating the inner loop logic. Use a clear grid-based layout. Color scheme: green for LIS elements and arrows, blue for the dp table, gray for non-selected elements, orange for the "max" operation highlight. Add a small label "longest increasing path" pointing to the highlighted chain. Educational infographic style, suitable for a 17-year-old audience learning algorithms for the first time.
