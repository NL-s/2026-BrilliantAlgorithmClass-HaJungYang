A clean educational illustration showing the concept of memoization in dynamic programming. On the left side, show a deeply branching recursion tree for fibonacci(5) with many duplicate subtrees highlighted in red to indicate redundant computation. On the right side, show the same recursion tree but with a yellow sticky-note "cache" attached to each node that has already been computed, and dashed arrows from duplicate nodes pointing back to the cached result instead of recomputing. Use a whiteboard or notebook aesthetic with hand-drawn style lines. Include small labels like "fib(3) already computed!" with an arrow. Color palette: soft blue for the tree nodes, red for duplicate nodes, yellow for cache notes, green for cache-hit arrows. The overall mood should feel like a friendly classroom diagram that a teacher drew to explain the concept. No text blocks, only minimal labels directly on the diagram.
