An educational step-by-step diagram of Kruskal's Minimum Spanning Tree algorithm. Show a graph with 5 numbered nodes and 7 weighted edges on the left side. On the right side, display a sequence of 4 steps where edges are added one by one in ascending weight order: first the cheapest edge is highlighted in green (added), then another cheap edge in green, then an edge that would create a cycle is shown in red with an X mark (rejected), and finally the MST is shown with all selected edges in bold green. Include a small sorted edge list at the bottom. Use a white background with green for selected edges, red for rejected edges, and gray for unused edges. Add Korean labels for "추가" (add) and "사이클 → 건너뜀" (cycle, skip). Clean, textbook-quality illustration for high school students.
