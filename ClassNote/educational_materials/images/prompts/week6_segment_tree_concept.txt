A clean educational diagram showing the concept of a Segment Tree built on a small array of 8 numbers. On the left side, show a flat horizontal array labeled "배열" with 8 colored boxes containing numbers, with a bracket underneath indicating a slow O(N) range-sum query by looping through each element. On the right side, show the same 8 numbers organized into a binary tree structure labeled "세그먼트 트리", where each parent node displays the sum of its two children's ranges, with green highlight arrows showing only 3 nodes being visited for the same range query. Between the two sides, draw a bold arrow labeled "build O(N)" pointing right. Use a light pastel background, soft shadows on nodes, and Korean labels for key concepts such as "구간 합", "O(N) 느림", and "O(log N) 빠름". The overall style should be modern and textbook-friendly, suitable for a 17-year-old student audience.
