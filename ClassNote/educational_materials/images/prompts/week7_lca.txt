A clear educational diagram of LCA (Lowest Common Ancestor) using Binary Lifting on a 7-node tree. The left half shows the tree structure: node 1 at the top (root, depth 0), nodes 2 and 3 at depth 1, nodes 4, 5, and 6 at depth 2, and node 7 at depth 3 as a child of node 5. Edges are drawn as solid black lines; each node is a circle with the node number inside. Depth labels appear on the right side of the tree as "depth 0", "depth 1", etc. The right half shows the Sparse Table (Binary Lifting table) as a grid: rows labeled k=0, k=1, k=2 and columns labeled v=1 through v=7, with the ancestor value filled in each cell using a color gradient (lighter for small k, darker for large k). Two example LCA queries are highlighted with colored paths on the tree: LCA(4,6) drawn with a green dashed path going up from node 4 and node 6 meeting at node 1 (labeled "LCA=1"), and LCA(4,5) drawn with an orange dashed path meeting at node 2 (labeled "LCA=2"). The overall style is a modern Korean algorithm textbook illustration, white background, clean layout with Korean labels "이진 리프팅 테이블" for the table and "트리 구조" for the tree section.
