A step-by-step educational diagram showing the Monotonic Deque (sliding window maximum) algorithm on the array [3, 1, 2, 5, 4, 6, 2, 3] with window size K=3. The diagram has 6 rows, one for each completed window position. Each row shows: the input array with the current window highlighted in a colored rectangle, the current state of the deque drawn as a horizontal queue with index numbers inside boxes (front on the left, back on the right), and the current maximum value circled in bright yellow on the right. Red arrows pointing left indicate elements being popped from the back of the deque when a larger element arrives, with a small Korean label "더 작으므로 제거" (removed because smaller). Blue arrows indicate elements expelled from the front when they leave the window, labeled "범위 초과". The final row summarizes the output sequence: 3, 5, 5, 6, 6, 6 in large bold orange numbers. The style is a bright classroom whiteboard aesthetic with a white background, clear sans-serif Korean and numeric labels, and the deque drawn as a rectangular slot machine-style container.
