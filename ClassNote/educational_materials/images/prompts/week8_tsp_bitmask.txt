An educational visualization of the Travelling Salesman Problem solved with bitmask dynamic programming. On the left side, show a small graph with 4 numbered cities (0, 1, 2, 3) connected by weighted edges, with edge weights labeled clearly. On the right side, show a DP table where rows are labeled with bitmask states (0001, 0011, 0101, 0111, 1111, etc.) and columns are labeled with the last visited city (0, 1, 2, 3). Highlight the state transitions with colored arrows showing how dp[mask | (1<<v)][v] is updated from dp[mask][u]. Use a clean grid layout with soft background colors, bold numbers, and annotation arrows. The bottom of the image should show the optimal path highlighted in the graph with a thick colored line. Style should be clear and readable for a 17-year-old student, using a flat modern infographic aesthetic with Korean captions where appropriate.
